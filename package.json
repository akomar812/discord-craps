{
  "name": "discord-craps",
  "version": "1.0.0",
  "description": "A discord bot for playing craps",
  "main": "index.js",
  "scripts": {
    "start": "node index.js | tee -a \"logs/discord-craps.$(date).log\"",
    "test": "jest --coverage --verbose --detectOpenHandles",
    "lint": "npx eslint ."
  },
  "author": "Andrew Komar",
  "license": "gpl-3.0",
  "dependencies": {
    "discord.js": "^12.5.3",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "eslint": "^7.29.0",
    "jest": "^27.0.5"
  }
}
